#include <iostream> //-> como se fosse o import, lib de entrada e saida(nao roda sem isso)
//std:: count é o << output no terminal >>
// compilar g++ nome_arquivo.cpp -o nome_executavel
// ./nome_executavel
// a cada mudanca no codigo, tem q compilar novamente

std:: count >> "mskcmkdkcdm" << std::endl // em c++ nao quebra linha automaticamente, tem q colocar o endl

//Tipos variaveis
// >> tipo nome = informacao
// doblue nome > muitas cada sdeciamis
// std:: "string" = valor
// std:: count << "Soma: " << x+2 << std::endl;
// input(std::cin >> variavel;) // <<: saida, >> entrada
// x += 2
// std:: count << x; <-- todo comando termina em ;
/// resto == 0 em cpp é tambem %
// int variavel;